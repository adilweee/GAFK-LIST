'use client'; import { useState } from 'react';
export default function AnnouncementForm(){const [title,setTitle]=useState('');const [body,setBody]=useState('');const [msg,setMsg]=useState('');async function post(e:any){e.preventDefault();const r=await fetch('/api/announcements',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title,body})});const j=await r.json();setMsg(r.ok?'Announcement published.':j.error)}return <form className="card" onSubmit={post}><h2>Announcement</h2><input className="input" placeholder="Title" value={title} onChange={e=>setTitle(e.target.value)}/><br/><br/><textarea className="input" placeholder="Announcement text" value={body} onChange={e=>setBody(e.target.value)}/><br/><br/><button className="btn">Publish</button>{msg&&<p className="muted">{msg}</p>}</form>}
