'use client'; import { useEffect, useState } from 'react';
export default function ThemeToggle(){const [theme,setTheme]=useState('dark');useEffect(()=>{const t=localStorage.getItem('gafk-theme')||'dark';setTheme(t);document.documentElement.dataset.theme=t},[]);function change(t:string){setTheme(t);localStorage.setItem('gafk-theme',t);document.documentElement.dataset.theme=t}return <select className="theme-select" aria-label="Theme" value={theme} onChange={e=>change(e.target.value)}><option value="dark">Dark</option><option value="purple">Purple</option><option value="blue">Blue</option></select>}
