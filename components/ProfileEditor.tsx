'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function ProfileEditor({profile}:{profile:any}){const [avatar_url,setAvatar]=useState(profile.avatar_url||'');const [banner_url,setBanner]=useState(profile.banner_url||'');const [bio,setBio]=useState(profile.bio||'');const [msg,setMsg]=useState('');const r=useRouter();async function save(){const x=await fetch('/api/profile',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({avatar_url,banner_url,bio})});const j=await x.json();setMsg(x.ok?'Profile saved.':j.error||'Could not save.');if(x.ok)r.refresh();}return <section className="card" style={{marginBottom:18}}><h2>Customize profile</h2><input className="input" placeholder="Avatar image URL" value={avatar_url} onChange={e=>setAvatar(e.target.value)}/><br/><br/><input className="input" placeholder="Banner image URL" value={banner_url} onChange={e=>setBanner(e.target.value)}/><br/><br/><textarea className="input" maxLength={300} placeholder="Short bio" value={bio} onChange={e=>setBio(e.target.value)}/><br/><br/><button className="btn" onClick={save}>Save profile</button>{msg&&<p className="muted">{msg}</p>}</section>}
