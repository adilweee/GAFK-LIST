import {NextResponse} from 'next/server';import {currentProfile} from '@/lib/auth';import {createAdminClient} from '@/lib/supabase-admin';
export async function POST(req:Request){const p=await currentProfile();if(!p)return NextResponse.json({error:'Login required.'},{status:401});const b=await req.json();if(!b.gd_level_id||!b.level_name||!b.youtube_url)return NextResponse.json({error:'Level ID, name and YouTube proof are required.'},{status:400});const db=createAdminClient();const {error}=await db.from('submissions').insert({submitter_id:p.id,...b,status:'pending'});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
