import { NextResponse } from 'next/server'; import { currentProfile } from '@/lib/auth'; import { createAdminClient } from '@/lib/supabase-admin';
export async function POST(req:Request){const p=await currentProfile();if(!p||p.role!=='owner')return NextResponse.json({error:'Owner only.'},{status:403});const {title,body}=await req.json();if(!title||!body)return NextResponse.json({error:'Title and text are required.'},{status:400});const {error}=await createAdminClient().from('announcements').insert({title,body,author_id:p.id});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true});}
