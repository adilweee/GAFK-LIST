import { NextResponse } from 'next/server'; import { currentProfile } from '@/lib/auth'; import { createAdminClient } from '@/lib/supabase-admin';
export async function POST(req:Request){const p=await currentProfile();if(!p)return NextResponse.json({error:'Login required.'},{status:401});const {reason,level_id}=await req.json();if(!reason)return NextResponse.json({error:'Reason is required.'},{status:400});const {error}=await createAdminClient().from('reports').insert({reporter_id:p.id,level_id:level_id||null,reason});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true});}
