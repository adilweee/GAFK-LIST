import {NextResponse} from 'next/server';import {currentProfile} from '@/lib/auth';import {createAdminClient} from '@/lib/supabase-admin';import {logActivity} from '@/lib/activity';
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const p=await currentProfile();if(!p||!['owner','moderator'].includes(p.role))return NextResponse.json({error:'Forbidden'},{status:403});const {id}=await params;const {error}=await createAdminClient().from('reports').update({status:'resolved'}).eq('id',id);if(!error)await logActivity(p.id,'resolved report','report',id);return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true});}
