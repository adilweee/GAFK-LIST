'use client';
import {useState} from 'react';
export default function Submit(){const [form,setForm]=useState({gd_level_id:'',level_name:'',creator:'',verifier:'',youtube_url:''});const [msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const r=await fetch('/api/submissions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const j=await r.json();setMsg(r.ok?'Submitted! It is now waiting for Owner review.':j.error)}return <main className="wrap"><form className="form card" onSubmit={go}><h1>Submit Level</h1>{Object.entries(form).map(([k,v])=><div key={k} style={{marginBottom:12}}><label className="muted">{k.replaceAll('_',' ')}</label><input className="input" value={v} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<button className="btn">Submit</button>{msg&&<p>{msg}</p>}</form></main>}
