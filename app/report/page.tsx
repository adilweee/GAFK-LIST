'use client'; import { useState } from 'react';
export default function Report(){const [reason,setReason]=useState('');const [msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const r=await fetch('/api/reports',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({reason})});const j=await r.json();setMsg(r.ok?'Report sent.':j.error)}return <main className="wrap"><form className="form card" onSubmit={submit}><h1>Report</h1><p className="muted">Report incorrect level information, proof or other problems.</p><textarea className="input" placeholder="Describe the problem" value={reason} onChange={e=>setReason(e.target.value)}/><br/><br/><button className="btn">Send report</button>{msg&&<p>{msg}</p>}</form></main>}
