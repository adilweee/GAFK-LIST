'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function Register(){const [u,setU]=useState(''),[p,setP]=useState(''),[e,setE]=useState(''),[mail,setMail]=useState('');const r=useRouter();async function go(x:any){x.preventDefault();const res=await fetch('/api/auth/signup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:u,password:p,email_optional:mail||null})});const j=await res.json();if(!res.ok)return setE(j.error);r.push('/login')}return <main className="wrap"><form className="form card" onSubmit={go}><h1>Create account</h1><p className="muted">Google/Gmail account is not required.</p><input className="input" placeholder="Unique username" value={u} onChange={x=>setU(x.target.value)}/><br/><br/><input className="input" type="password" placeholder="Password (8+ characters)" value={p} onChange={x=>setP(x.target.value)}/><br/><br/><input className="input" type="email" placeholder="Optional email" value={mail} onChange={x=>setMail(x.target.value)}/><br/><br/>{e&&<p style={{color:'var(--danger)'}}>{e}</p>}<button className="btn">Register</button></form></main>}
